<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.weibo.mapper.FollowMapper">

    <insert id="follow">
        insert into follow(follower_id, following_id) values(#{followerId}, #{followingId})
    </insert>

    <delete id="unfollow">
        delete from follow where follower_id = #{followerId} and following_id = #{followingId}
    </delete>

    <select id="check" resultType="com.weibo.entity.Follow">
        select * from follow where follower_id = #{followerId} and following_id = #{followingId} limit 1
    </select>

    <select id="countFollowers" resultType="int">
        select count(*) from follow where following_id = #{userId}
    </select>

    <select id="countFollowing" resultType="int">
        select count(*) from follow where follower_id = #{userId}
    </select>

    <select id="getFollowerIds" resultType="int">
        select follower_id from follow where following_id = #{userId}
    </select>

    <select id="getFollowingIds" resultType="int">
        select following_id from follow where follower_id = #{userId}
    </select>

    <select id="getFollowers" resultType="com.weibo.entity.User">
        select u.id, u.username, u.avatar from user u
        join follow f on u.id = f.follower_id
        where f.following_id = #{userId}
    </select>

    <select id="getFollowing" resultType="com.weibo.entity.User">
        select u.id, u.username, u.avatar from user u
        join follow f on u.id = f.following_id
        where f.follower_id = #{userId}
    </select>

    <select id="getRecommendUserIds" resultType="int">
        select u.id from user u
        where u.id != #{userId}
          and u.id not in (select following_id from follow where follower_id = #{userId})
        order by (select count(*) from follow where following_id = u.id) desc
        limit 5
    </select>

</mapper>
